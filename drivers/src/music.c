#include "../inc/music.h"
/*double frequencies[8];
int volume=0x000000FF;

void fillFIFO() {
    
	int index;
	int signal;
    int i,j=0;

    for(i=0; i<144000; i++)
    {
    	for(j=0; j<8; j++)
		{
			if((int)frequencies[j])
			{	
					index = ((int)frequencies[j] * i) % 48000;
					signal += volume * sine[index];
			}
			
		}
		audio_write_data_ASM(signal, signal);
		signal=0;
	    
   }

   return;
}*/
	
    
